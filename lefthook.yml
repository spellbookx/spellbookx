pre-commit:
  piped: true
  commands:
    prettier:
      run: pnpm prettier --ignore-unknown --write {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    lint-commit-msg:
      run: pnpm exec commitlint --edit {1}
